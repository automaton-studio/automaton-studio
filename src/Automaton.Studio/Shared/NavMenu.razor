@using Automaton.Studio.Services

<Menu Mode=MenuMode.Inline Theme=MenuTheme.Light Selectable=true DefaultSelectedKeys=@(new[]{"1"})>
    <MenuItem Key="1" Icon="profile" RouterLink="/"> 
        Flows
    </MenuItem>
    <MenuItem Key="2" Icon="apartment" RouterMatch="NavLinkMatch.Prefix" RouterLink="/flowdesigner/" Disabled=NavMenuService.IsDesignerDisabled()>
        Flow Designer
    </MenuItem>
    <MenuItem Key="3" Icon="experiment" RouterMatch="NavLinkMatch.Prefix" RouterLink="/stepdesigner/">
        Step Designer
    </MenuItem>
    <MenuDivider />
    <MenuItem Key="4" Icon="user" RouterMatch="NavLinkMatch.Prefix" RouterLink="/account/">
        Account
    </MenuItem>
</Menu>

@code
{
    [Inject] public NavMenuService NavMenuService { get; set; }
}

